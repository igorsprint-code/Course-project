- name: restart node exporter
  ansible.builtin.systemd:
      name: node-exporter
      state: started

- name: daemon reload
  ansible.builtin.systemd:
    daemon_reload: yes

- name: enable node-exporter
  ansible.builtin.systemd:
    name: node-exporter
    enabled: yes
    masked: no
    
