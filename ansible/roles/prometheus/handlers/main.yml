- name: daemon reload
  ansible.builtin.systemd:
    daemon_reload: yes

- name: restart prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: restarted


- name: enable prometheus
  ansible.builtin.systemd:
    name: prometheus
    enabled: yes
    masked: no

