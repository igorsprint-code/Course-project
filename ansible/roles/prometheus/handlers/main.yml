- name: daemon reload
  systemd:
    daemon_reload: yes

- name: restart prometheus
  service:
    name: prometheus
    state: restarted


- name: enable prometheus
  systemd:
    name: node-exporter
    enabled: yes
    masked: no

